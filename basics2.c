// FUNCTIONS DECLARATIONS, DEFINTIONS AND CALLING

/*
#include <stdio.h>
 
    int add(int a, int b);  // FXN DECLARATION

    int add(int a, int b)   //FXN DEFINITION
    {
        printf("enter a number: ");
        scanf("%d", &a);

        printf("enter another number: ");
        scanf("%d", &b);

        int sum= a +b;
        return sum;
    }
    
    int main()
    {
        int a,b;
        int result= add(a,b);

        printf("The addition will result in: %d",  result);

        return 0;
    }

    */
/*
    #include <stdio.h>

    int factorial(int n);

    int factorial(int n)
    {

        if( n == 0 ||  n == 1)
       { 
        return 1;
       }

        else
        {
            return n * factorial(n-1);
        }
    }

    int main()
    {
        int n;
        printf("Enter any number: ");
        scanf("%d", &n);

        int result = factorial(n);
        printf("The factorial of given number is : %d", result);

        return 0;
    }

    */

   /*

   #include <stdio.h>

   int main(){

    int numbers[5] = {1,2,3,4,5};

    for(int i=0; i<=5; i++)
    {
        printf("%d", numbers[i]);
    }
    return 0;

   }

   */
/*
  #include <stdio.h>

  void printArray(int arr[], int size)
  {
    for(int i = 0; i < size; i++)
    {
        printf("%d ", arr[i]);
    }
  }

  int main() {

    int numbers[5] = {10, 20, 30, 40, 50};
    printArray(numbers, 5);
    return 0;
  }
  */

 #include <stdio.h>
 